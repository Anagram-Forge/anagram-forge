# anagram-forge-mail

Tiny Cloudflare Worker. Accepts a sponsor-form POST and sends mail to
`sponsors@anagramforge.com` (Email Routing forwards that to the inbox).

This is a **Worker**, not a Pages project. Send Email bindings only work here.
